import React from 'react';
import "./Spinner.scss"

const Spinner = () => {
    console.log("SPINNERRR!")
    return (
        <div className="Spinner">
            
        </div>
    );
};

export default Spinner;